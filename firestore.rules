rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Yêu cầu đăng nhập mới được đọc/ghi dữ liệu
      allow read, write: if request.auth != null;
    }
  }
}
